# Copy this file to `.env.local` and adjust values.
# `.env.local` is git-ignored by Next.js convention.

# Backend API base URL (local dev backend runs on port 8080 with context path /api)
NEXT_PUBLIC_API_URL=http://localhost:8080/api
