# Additional Packages for Homepage Features

## Install required packages:

```bash
cd frontend

# For carousel/slider (optional - we'll use pure CSS instead)
# npm install swiper

# For icons (if not already installed)
# npm install lucide-react
```

## Note:
To keep it simple, I'll implement features using pure CSS and React hooks instead of external libraries.
This keeps bundle size small and gives you more control.
